import 'package:flutter/material.dart';

class Mensajes extends StatelessWidget {
  
  Mensajes({this.nombre , this.text , this.remitente});

  final String nombre;
  final String text;
  final bool remitente;

  @override
  Widget build(BuildContext context) {
    return Container(
      child: child,
    );
  }
}